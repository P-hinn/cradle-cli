/**
 * Library entry point.
 *
 * `core/` functions take their inputs as arguments and return values — they never
 * touch the filesystem or the console. File access and output live in `cli/`.
 * See SPEC.md §8.
 */

export * from './types/index.js'
